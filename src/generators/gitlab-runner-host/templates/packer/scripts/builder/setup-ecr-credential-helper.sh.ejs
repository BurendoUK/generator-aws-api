function die (){
  echo "$1" 1>&2
  exit 1
}

echo "Started running $(basename "$0")"

echo "> Creating .docker folder"
mkdir ~/.docker
sudo runuser -l root -c 'mkdir ~/.docker'

echo "> Copying docker config file"
cp /tmp/docker-config.json ~/.docker/config.json
sudo runuser -l root -c 'cp /tmp/docker-config.json ~/.docker/config.json'

# Got this far? Then we are good
echo "Finished running $(basename "$0")"
exit 0;
