function die (){
  echo "$1" 1>&2
  exit 1
}

echo "> Installing pipenv"
sudo -H  pip install pipenv
[ $? -ne 0 ] && die "Failed to install pipenv";

# Got this far? Then we are good
echo "Finished running $(basename "$0")"
exit 0;
