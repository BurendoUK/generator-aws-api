import setuptools

setuptools.setup(
    name='<%=name%>',
    version='0.1.0',
    description='',
    url='#',
    author='',
    install_requires=[],
    author_email='',
    packages=setuptools.find_packages()
)
