{
  "name": "generator-aws-api",
  "version": "0.1.0",
  "description": "Scaffold AWS api-gateway using terraform",
  "scripts": {
    "build": "rm -rf ./dist && tsc -b && npm run copy:files",
    "copy:files": "copyfiles -u 1 ./src/**/*.ejs ./dist/ && copyfiles package.json ./dist/",
    "generator:local": "ts-node node_modules/.bin/yo",
    "link": "pushd dist && npm link && popd"
  },
  "files": [
    "generators"
  ],
  "keywords": [
    "yeoman-generator"
  ],
  "author": "chris@unchain.life",
  "license": "ISC",
  "dependencies": {
    "js-yaml": "^4.1.0",
    "yeoman-generator": "^5.4.2"
  },
  "devDependencies": {
    "@types/node": "^18.7.5",
    "@types/yeoman-generator": "^5.2.11",
    "copyfiles": "^2.4.1",
    "prettier": "^2.7.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4",
    "yo": "^4.3.0"
  }
}
